/* eslint-env jasmine */

// import {globRx} from '../src/glob-rx'

describe('glob-rx', () => {
  describe('with default files', () => {
    it('should return expected file list', (done) => {
      done();



    })
  })
})
